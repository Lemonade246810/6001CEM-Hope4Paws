import { Text, View } from "react-native";

export default function AssignedReports() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Assigned Reports Screen</Text>
    </View>
  );
}
